Release notes
KrankyBear ProcessMiner -- cross-platform (Windows, Linux, macOS) process/system monitor

Future ideas:
- Persist watched processes/directories in Preferences across launches, with a reactivation flow at startup (see/choose to reactivate, edit, or leave disabled). Bigger than it first looks: PIDs aren't meaningful across a restart, so a process watch would need to persist by *name* (or exe path) instead and re-match against whatever's actually running next launch -- straightforward for directory watches (just the path), needs real design for process watches (what if two processes share a name? what if it never comes back?). The auto-open/focus half of this same idea shipped already (see 0.3.0 below); this is just the persistence/reactivation half.

- ETW-based work, Phase 2 continued: real per-process network usage and I/O latency -- the minifilter/AV-interference third of Phase 2 shipped AND is confirmed fully working in 0.5.0 (Interference Watch's 4th signal + trust-eval fallback, verified end-to-end against real Chrome/Notepad activity, not just a synthetic spike -- see 0.5.0 below), and turned out not to need any hand-rolled StartTrace/TDH bindings after all -- github.com/tekert/goetw (pure Go, no CGO) already wraps that, and the relevant providers (Microsoft-Antimalware-*) are modern and well-documented. Network usage and disk I/O latency are the genuinely hard remainder, still not started: confirmed empirically this round that WPR's Network/DiskIO profiles pull from the classic, MOF-based "NT Kernel Logger" mechanism instead (unnamed provider GUIDs even wevtutil can't resolve to a friendly name) -- a materially different, more special-cased API (SystemTraceControlGuid + EnableFlags bitmasks, not EnableTraceEx2-by-provider-GUID) with unconfirmed support even in goetw (which does expose NewKernelRealTimeSession/NewSystemTraceSession -- worth checking first, not assumed either way).

- Per-process open-handles/files window (listing each individual open handle/fd with what it actually points to, not just the count the Handles column already shows) -- promoted here from "some day maybe" after real use: the 0.4.0 Handles column immediately surfaced a process with a surprisingly high count, which the count alone can't explain -- you need the list of *what* to actually debug it. Feasibility is uneven across platforms, though: Linux is easy (/proc/[pid]/fd already resolves each entry to a path); macOS is moderate (the same proc_pidinfo call the count uses can list each fd, and proc_pidfdinfo/PROC_PIDFDVNODEPATHINFO resolves file-backed ones to a path, no root needed for your own processes); Windows is the hard one -- undocumented NtQuerySystemInformation(SystemHandleInformation) plus per-handle NtQueryObject name resolution, and querying certain handle types (notably a named pipe with no listener) can hang, needing a timeout/worker-thread workaround. Comparable in size to the thread-list/stack-scan work already shipped, not a quick add.

- User option show additional column for signed or unsigned processes with a simple icon, or unicode checkmark, whatever is lowest resource use? Maybe minimize impact by performing background check after launch and populate as possible, and when new processes are detected. Maybe not that valuable, but signed vs unsigned might also be useful in troubleshooting if not a substantial burden. Recognizing this is Windows only with authenticode.


Some day maybe, maybe never - defer for now
- Network user per process - unlikely since gopsutil does not have access to this.
- GPU resource use - Windows only since not readily available for Mac/Linux, maybe some day if we perceive value
- Process CPU affinity / priority controls -- scrapped rather than deferred: affinity has no real macOS support at all (Apple's affinity API is advisory-only, a non-starter on Apple Silicon), and priority is a rare power-user action for most people. Not worth the cross-platform inconsistency for the value.
- ntdll.dll hook-byte comparison (on-disk vs in-memory at known Nt* API entry points) + unbacked-executable-memory-region scan, Windows only -- the actual "prove an AV/EDR hook or malware patched something" signal, bigger than the thread-list-with-start-address-resolution shipped in 0.3.0. Revisit once that's proven useful in practice.

Version 0.5.0 - August 10, 2026
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ NEW Interference Watch's 4th signal: Defender file-scan monitoring
   (ETW Phase 2, Windows-first)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Interference Watch now has a fourth detection signal, live: Windows
  Defender's own AMFilter minifilter intercepting a file a watched process
  opens -- the *other* meaning of "AV interference" (synchronous file-scan
  latency), which the original three signals can't see at all since they
  only look at what's happening inside the watched process itself, not at
  it from outside. Shown as a plain "⚠", never "🛑" (Defender doing its job
  is expected, not an accusation), and unlike the other three, logged every
  time it happens rather than tracked against a baseline, since a file
  scan is a momentary event, not a persistent condition.
- This is genuinely live, in-app ETW consumption -- not a recording like
  Capture Trace -- via a real-time subscription to
  Microsoft-Antimalware-AMFilter using github.com/tekert/goetw (pure Go,
  no CGO, GPL-3.0 -- matching this project's own license). Needed real
  research to scope correctly: a throwaway spike against the real hardware
  (subscribing to all four Antimalware providers, deliberately triggering
  activity via this project's own test/injection tooling, and inspecting
  ~18,000 captured events) found that only AMFilter's own FileScan task
  reliably carries the *scanning-triggered* process's real PID in the
  event header -- Microsoft-Antimalware-RTP's equivalent task has the
  *engine's* PID (MsMpEng.exe) instead, unusable for correlation without
  extra work, and Behavior Monitoring events (which, notably, captured
  this project's own injection tooling's exact OpenProcess call in loving
  detail) are far higher-volume and system-wide, out of scope for this
  pass.
- Needs Administrator rights to create the real-time session at all,
  confirmed empirically (same requirement Capture Trace already has) --
  silently just doesn't activate if not running elevated, the same way
  the other three signals are silently inert on macOS/Linux. Not a
  deliberate user action with its own dialog, so no error is ever shown
  for "it didn't start" -- Interference Watch's other three signals work
  fine either way.
- The async ETW callback needed real synchronization to hand events to
  interferenceWatcher.check() (which runs on the main goroutine) without
  retrofitting a mutex onto that whole pre-existing, previously
  single-goroutine-only struct -- a small, separate, mutex-protected relay
  is the one new piece of state that's actually touched from two
  goroutines, confirmed race-free with `go build -race` on the real
  hardware (the same lesson Capture Trace's corrupted-.etl bug taught
  earlier this release, applied proactively here instead of found the
  hard way again).
- Also confirmed safe: a stale ETW session left behind by a force-killed
  prior instance (simulating a crash) doesn't hang or crash a fresh
  launch -- it just silently fails to (re-)start the 4th signal for that
  session, same "best-effort, no dialog" behavior as any other failure
  here.
- Found via real-world testing (watching Chrome, then Notepad, downloading
  files, saving files): the file-scan signal never fires at all for a
  well-known, Microsoft-signed "trusted" process -- Notepad specifically
  produced zero AMFilter_FileScan events despite a confirmed, completed
  Save, while Defender's own trust bookkeeping (a *different* AMFilter
  task, "AMFilter_TrustedProcess") fired reliably for other processes in
  the same window. Defender appears to fast-track trusted processes
  through a code path that skips the file-scan event entirely. Added a
  fallback for exactly this: a process being registered for trust
  evaluation is now also logged (no file path -- a different, narrower
  claim than "this file was scanned") -- so a trusted process still shows
  *something* in the log rather than going silent. Needed its own
  real-world-verified schema too: the correlating PID for this task lives
  in the event *payload* ("Pid"), not the header's ProcessID the file-scan
  signal uses (that field is the Defender component emitting the event,
  e.g. MsMpEng.exe, not the subject process) -- an easy mistake this
  session's habit of verifying against real captured events, not
  documentation or assumption, caught before it shipped wrong.
- Also found along the way (a thread-stack hit against a Chrome child
  process mid-download, showing Defender's own on-access-scan module):
  added mpclient/mpoav to the known-security-module list so a hit like
  that shows the friendly "Microsoft Defender" label instead of reading
  as an unrecognized third-party module.
- Also fixed: an early-return ordering bug that would have skipped the
  whole 4th-signal block whenever threadStartAddressSupported is false,
  contradicting this signal's own documented independence from that flag
  -- harmless on Windows today (where that flag is always true), but
  caught and fixed while re-reading the code during this same round of
  real-world testing rather than left for later.
- Confirmed fully working end-to-end on real hardware, not just via
  synthetic spikes: watching Chrome (via a directory watch, to cover
  whichever of its several child processes actually handles a download)
  and Notepad (via Save) live in the app, both the file-scan signal
  (Notepad's own icon-cache and tab-state files, Chrome's downloads) and
  the trust-evaluation fallback (Chrome) fired correctly with the right
  PIDs and text, and the mpclient/mpoav known-module fix showed the
  correct "Microsoft Defender" label instead of an unrecognized-module
  warning.
- Network usage and disk I/O latency remain a separate, harder, not-yet-
  started piece of Phase 2 -- see Future ideas.

✨ NEW "Capture Trace" (ETW recording via WPR, Windows-first)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- New View menu/tray item, Windows-only: record an ETW trace to a .etl via
  Windows Performance Recorder (wpr.exe, built into Windows). Four
  checkboxes, checked by default -- Network, Disk I/O, File I/O, and
  Minifilter (the *other* meaning of "AV interference" this app has
  documented as needing ETW, since Interference Watch's own three signals
  only cover what happens inside the watched process itself). Start, then
  Stop && Save or Cancel.
- Needed zero custom ETW/TDH code -- WPR already has first-class built-in
  profiles for exactly these gaps, confirmed via `wpr.exe -profiles` on a
  real machine, so this is a thin wrapper around the same tool, not a
  from-scratch trace consumer. Verified end-to-end on real hardware: a
  genuine start/stop/save round trip produced a real, non-empty .etl
  (~700MB for well under a minute with Network+File I/O alone -- worth
  knowing before starting a longer capture), and Cancel safely handles
  being called twice in a row.
- Needs Administrator rights -- confirmed via real-world testing on this
  exact release: launched normally, Start failed with "Failed to enable
  the policy to profile system performance" (all four profiles are
  kernel-level). No in-app elevation prompt yet -- relaunch ProcessMiner as
  Administrator first (see the single-instance change right below for why
  that no longer means quitting your other, normal instance); the error
  message says so explicitly if you forget.
- Deliberately not a trace analyzer -- open the result in Windows
  Performance Analyzer (WPA) instead; see README's Known limitations for
  why a built-in one isn't planned. This is "ETW work, Phase 1" -- live
  in-app network/I/O/interference consumption is a separate, much bigger
  Phase 2, not started (see Future ideas).

🔧 Single-instance enforcement now allows one normal + one elevated
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Windows-only relaxation of the "always exactly one instance" rule, added
  specifically for Capture Trace: a second copy is now allowed if its
  elevation differs from the first's (one normal + one elevated, either
  order) -- so using Capture Trace no longer means quitting your regular,
  already-running instance first. Two at the *same* elevation level still
  isn't allowed -- that's the actual resource-doubling concern the rule
  exists for, unchanged. Checks each instance's own token via
  windows.Token.IsElevated() (already wrapped in golang.org/x/sys/windows,
  no hand-rolled bindings needed here) -- PROCESS_QUERY_LIMITED_INFORMATION
  is the same access level Task Manager's own "Elevated" column relies on
  to report on other processes without itself needing elevation.
- Elevated instances now say so in every window's title bar
  ("Administrator: KrankyBear ProcessMiner"), Windows' own convention for
  elevated console windows -- added once running elevated became a real,
  everyday mode for this app rather than an internal detail.
- Capture Trace now checks elevation upfront: opening the window without
  Administrator rights shows an explanation and a one-click "Relaunch as
  Administrator" button instead of the checkboxes and Start button, rather
  than letting you click through a whole round trip just to hit the same
  admin-rights error every time. The button (ShellExecute's "runas" verb,
  a real UAC prompt) launches a second, elevated copy without closing this
  one -- only possible cleanly because of the single-instance relaxation
  right above; declining the UAC prompt is treated as a normal choice, not
  an error, so nothing happens rather than showing a scary dialog.

🐛 FIXED "already running" alert window rendering far too tall
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Found via real-world testing on Windows: triggering the "already
  running" dialog (e.g. by trying to launch a third instance) rendered an
  extremely tall, narrow window -- a Fyne wrapped-label minimum-size
  calculation issue that surfaced once the elevation-aware message text
  above grew past a couple of short lines. Shortened the message and
  pinned the window to a fixed size rather than chasing the exact layout
  cause for what's just a small alert dialog.
- Also found via the same testing: the "already running" window is its own
  separate process with no connection to whichever instance(s) it's
  complaining about, so it doesn't close when those do -- it was sitting
  around as an orphaned leftover until dismissed manually. Now polls the
  same check it was built from every couple of seconds and closes itself
  once it's no longer true. Still no real cross-process IPC, just noticing
  the process list changed, the same way the check itself already works.

🐛 FIXED Capture Trace corrupting the .etl if you exit mid-save
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Found the hard way via real-world testing: exiting ProcessMiner while
  Stop && Save's `wpr -stop` was still merging a trace corrupted the .etl
  (WPA refused to open the result). Root cause: quitApp's cleanup step
  unconditionally called `wpr -cancel` to tear down any still-running
  capture, but the code tracking "is a session running" didn't distinguish
  "recording" from "currently being saved" -- so quitting mid-save raced
  an actual `wpr -cancel` against the still-in-flight `wpr -stop`, and losing
  that race is exactly what corrupted the file. Fixed at the root: cleanup
  now only cancels a session that's just sitting there recording, never one
  mid-save or already finished -- letting a save that's already in flight
  finish on its own in the background even if you exit (wpr.exe is a
  separate process Windows doesn't tie to this app's lifetime), rather than
  actively sabotaging it. Needed a real mutex, not just careful ordering --
  this state is genuinely touched from two different goroutines (the save's
  own background goroutine and quitApp's main-goroutine cleanup), confirmed
  by rebuilding with `go build -race` on the real hardware.
- Also made the "Saving…" status impossible to miss while this window
  exists: red/danger-styled text explicitly warning not to exit until it
  finishes. Exiting still can't be blocked outright, so this is the
  practical mitigation on top of the actual fix above.

🐛 FIXED Hide All / Show All missing the Threads and Children windows
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Found via real-world testing on Windows: Hide All (Alt+H) / Show All
  already covered About/Help/Update/System Info/Resource Details/
  Interference Watch, but not a Threads window or a Children drill-down --
  they'd stay sitting on screen through a Hide All, and not come back with
  the rest through a Show All. Same package-level window-tracking pattern
  the other windows already use, just not wired up for these two yet.


Version 0.4.0 - August 08, 2026
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ NEW "Top Disk" consumer filter on the main table
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Resource Details' own Top Consumers panel has had a Disk threshold
  (≥10/100/500/1000 KB/s) since 0.2.0; the main table's "Top CPU"/"Top Mem"
  selects had no Disk equivalent, even though per-process Disk R/W was
  already being sampled every tick -- a real inconsistency, now closed.
  Same tiers, same widget, same OR-combined semantics as CPU/Mem (a
  process shows if it clears *any* one of the enabled thresholds, not
  all of them).
- Corrected a stale code comment while in there: per-process disk I/O does
  work on macOS via gopsutil (confirmed empirically earlier this project),
  contrary to what an old comment still claimed.

✨ NEW "Top Memory" consumer filter (absolute bytes, not just percent)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- "Top Mem" filters by percentage, which answers a different question than
  "how much memory, in absolute terms" -- 5% is nothing on a 128GB
  workstation and enormous on a 16GB laptop. New "Top Memory" select (Off /
  ≥100 MB / ≥500 MB / ≥1 GB / ≥4 GB) filters on the actual RSS byte value
  the new Memory column already shows, combined via OR with the other
  three Top filters same as always.

✨ NEW Clickable Top Consumers (Resource Details window)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Clicking a row in the Top Consumers panel now brings the main window
  forward with that process selected in the table, same as clicking a
  child in the detail pane's children list. Previously the panel was
  read-only -- useful for spotting what's driving CPU/Memory/Disk, but
  with no way to actually go look at the process it named.

🔧 Narrower main window
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- "Show Threads", "Watch for Interference", and "End Process" moved off
  the crowded filters row onto their own third toolbar row. All the Top
  CPU/Mem/Memory/Disk filters plus those three action buttons had grown
  too wide to fit a laptop display on one row; trading one row of process
  names for a shorter, non-scrolling toolbar is worth it.

🐛 FIXED Threads window going stale (or duplicating) when you select a
   different process
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Found via real-world use: the Children drill-down window already follows
  whatever process gets selected next (main table, Resource Details' Top
  Consumers); the Threads window didn't -- it kept showing the first
  process you opened it for, and clicking "Show Threads" again for a new
  selection just opened a confusing second Threads window instead of
  updating the first. Threads now behaves the same as Children: only one
  window, and it follows the selection automatically while it's open.

✨ NEW "Averaged over" rolling window (Resource Details' Top Consumers)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- The Top Consumers list previously ranked purely off the latest sample --
  fine for a process that's steadily heavy, but a bursty one (say, 80% CPU
  for a second every few seconds) could show up or vanish depending on
  exactly which instant you looked. New "Averaged over" select (Off / 10s /
  30s / 1 min / 2 min) ranks and filters by each process's average over
  that trailing window instead, with the header noting which window is
  active (e.g. "Top CPU consumers (30s avg)"). Off preserves the original
  instantaneous behavior as the default.

✨ NEW "Check Signature" (Authenticode/code-signing check, Windows-first)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- A different angle on the same trust-but-verify idea as Thread Inspection
  and Interference Watch, but aimed at the executable file itself rather
  than its runtime behavior. Select a process and click "Check Signature"
  for a one-off Authenticode check of its on-disk .exe: ✓ signed and
  trusted, ⚠ not signed at all (not necessarily malicious, but worth a
  second look), or 🛑 signed with a problem (hash mismatch, untrusted/
  self-signed or expired certificate).
- Also checks Windows' catalog-signing mechanism, not just an embedded
  signature -- found via real-world testing against this Windows box: most
  System32 binaries (e.g. notepad.exe) are catalog-signed rather than
  individually signed, so checking only for an embedded signature would've
  flagged a huge share of stock Windows as "unsigned," the opposite of
  useful. No revocation check (would mean a network fetch per check,
  against this app's offline-first design elsewhere) -- a structural check
  (is it signed, does the chain lead to a trusted authority), not a live
  "has this been revoked since" verdict.

✨ NEW "Handles" column (open handles / file descriptors, all 3 platforms)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- New main-table column: open handles on Windows, open file descriptors on
  macOS/Linux -- the same underlying idea everywhere, and unlike Private,
  genuinely available on all three: gopsutil's NumFDs() turned out to be a
  single cheap syscall on every platform (confirmed by reading its actual
  per-OS source, then empirically on both this Mac and the Windows box,
  before trusting it -- see CLAUDE.md's standing caution about assuming a
  gopsutil field is cheap). A count that climbs steadily and never comes
  back down, even while a process otherwise looks idle, is a classic sign
  of a handle/fd leak -- exactly the kind of thing behind a process slowly
  degrading or eventually hanging.

Version 0.3.0 - August 07, 2026
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ NEW Thread inspection ("Show Threads" button, Windows-first)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Select a process and click "Show Threads" for a one-off snapshot of its
  threads. On Windows: TID, state, kernel/user CPU time, and each thread's
  start address resolved to "module.dll+0xOFFSET" -- or flagged "UNBACKED
  (possible injection)" when the address falls outside every one of the
  process's own loaded modules. That's the classic signal for reflective
  DLL injection or shellcode (the same technique Process Explorer/Process
  Hacker-style tools use), and the main practical ask behind this feature:
  verifying a customer's AV/security exclusions are actually configured
  as claimed, not just trusting that they are.
- This detects code *injected* into a process. It does NOT (yet) detect an
  AV/EDR *hook* patched into an otherwise-legitimate module like ntdll.dll
  -- that needs comparing in-memory bytes against the on-disk DLL at known
  API entry points, a separate, bigger piece of work noted in "Some day
  maybe" below.
- Windows implementation: NtQuerySystemInformation(SystemProcessInformation)
  for the thread list (thread start addresses aren't available via the
  simpler Toolhelp API), cross-referenced against the process's loaded
  modules via the classic Toolhelp module snapshot. Verified for real on
  Windows via SSH during development, not just compiled: a standalone test
  program confirmed thread states, start addresses, and module address
  ranges all resolve correctly against a real running process (including
  a documented NT quirk where the actively-querying thread's own start
  address reports as 0 -- handled as "unresolved", not misflagged as
  "UNBACKED").
- macOS/Linux: just a thread count for now. Resolving start addresses
  needs reading another process's memory, which needs an entitlement
  Apple doesn't grant ordinary third-party apps on macOS (SIP blocks
  task_for_pid), or root/CAP_SYS_PTRACE on Linux (Yama's ptrace_scope
  blocks it otherwise) -- a real platform gap, not something to code
  around. Best-effort, matching this app's general philosophy of being
  honest about what each OS actually allows rather than faking parity.

✨ NEW Interference Watch (continuous, on top of Thread Inspection)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Thread Inspection above is a one-off snapshot; this makes the same
  detection continuous and a lot more approachable for someone who isn't a
  Windows internals expert. Select a process and click "Watch for
  Interference" (next to "Show Threads"), or watch a whole directory --
  any process launched from it is covered automatically without
  reselecting it every time it restarts.
- Every process-list sample, each watched process is re-checked; a newly
  appearing UNBACKED thread (one that wasn't there the first time this
  process was checked) is logged as an event -- process, time, TID, start
  address -- and flags a "⚠" on that row in the main table. A thread that
  was already unbacked *before* watching started is recorded as the
  baseline and not reported, so watching an unusual-but-legitimate process
  doesn't cry wolf on the first check.
- Deliberately opt-in, not "watch everything": scanning every process this
  way would mean a Toolhelp32 module snapshot per process on top of the
  shared system-wide thread query every cycle -- fine for a handful of
  watched targets, not hundreds. Matches the actual ask: verify specific
  exclusions, not replace the AV.
- "Check for Interference" (View/Process menu, tray) opens the
  Interference Watch window: what's currently watched (with Remove), an
  "Add Directory to Watch…" picker, and the interference event log with
  Copy to Clipboard / Clear. Included in Hide All / Show All like every
  other secondary window.
- The one syscall NtQuerySystemInformation(SystemProcessInformation)
  already returns the whole system's thread list regardless of how many
  processes are being checked against it, so watching 1 target or 20 costs
  the same one call per cycle -- no repeat of this app's own earlier
  Status()-forks-ps-per-call mistake, just against a different call.
  Directory matching resolves each process's on-disk executable path
  (gopsutil's Exe()) once per PID and caches it, rather than re-resolving
  it for every running process on every tick.
- Windows-only, same constraint as the thread inspection it's built on --
  the watch list can still be built on macOS/Linux, but nothing is ever
  flagged there (the button and the window's "Add Directory" say so
  directly rather than silently doing nothing).
- Does NOT detect the *other* common meaning of "AV interference" --
  synchronous file-scan latency from a minifilter driver intercepting file
  I/O, which shows up as slow I/O on the target process while the AV's own
  process burns CPU, not as anything inside the target's own threads. That
  needs a different, ETW-based mechanism; not implemented here.

✨ NEW Interference Watch: second detection signal, new module loaded
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- The UNBACKED-thread signal above catches *reflective* injection (raw
  shellcode, no module ever loaded) -- malware's usual technique for
  staying off the module list. It deliberately does NOT catch legitimate
  AV/EDR hooking, which almost always uses ordinary LoadLibrary-based DLL
  injection instead, specifically because it *wants* its DLL visible, not
  hidden. Confirmed by live testing on a real Chrome process: a
  CreateRemoteThread-based shellcode stub read back with a generic,
  unresolvable start address in *both* ProcessMiner and Process Explorer
  (neither tool flagged it), while a LoadLibraryW-based DLL injection --
  the more realistic stand-in for how a real hook actually behaves --
  showed up cleanly via this new signal.
- Same watch list, same baseline/"newly appeared since watching started"
  semantics as the thread signal: a module already loaded before you
  started watching is the baseline, not reported. Logged as its own event
  ("new module loaded: <name>"), and flags the same "⚠" in the main table.
- Costs a second Toolhelp32 module snapshot per watched target per cycle
  (separate from the one the thread signal already takes internally) --
  simplicity over micro-optimization, still bounded by the watch-list
  size, not the system-wide process count.
- Two real bugs found and fixed via this live testing, both now verified
  against a real injected module on a real Chrome process:
  - The "⚠" indicator only stayed lit for the one sample tick a new module
    was first detected, then went dark even though the module -- and the
    interference it represents -- was still loaded. Now stays flagged for
    as long as the condition persists, matching the thread signal.
  - The new-module check was silently skipped whenever the *separate,
    unrelated* system-wide thread-list query failed on a given cycle, even
    though it doesn't use that data at all. Two consecutive live
    DLL-injection tests went undetected before this was caught -- fixed by
    no longer gating one detection path on the other's success.
- Also fixed: the Parent-processes children drill-down window's own "⚠"
  column wasn't wired to the flagged-PID state at all, so a recycled table
  cell could show stale leftover text from whatever column it last
  rendered (spotted via a screenshot showing PID/CPU/Private values
  bleeding into the "⚠" column).

✨ NEW Interference Watch: third detection signal, thread stack scan
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Prompted by a real customer story: a Windows performance expert on the
  team caught a security team's undisclosed removal of required exclusions
  live, in front of the customer, by having them watch Process Explorer and
  scroll through threads and stacks -- "thread stacking," watching live
  call stacks for a security vendor's own code sitting in the call path.
  This is a coarse, address-scanning approximation of that technique: for
  each of a watched process's threads, reads the thread's entire committed
  stack region (via its TEB's StackBase/StackLimit -- no thread suspension
  needed, unlike reading live register state would require) and checks
  every pointer-aligned value against the process's loaded-module address
  ranges. A hit against a module that isn't part of Windows itself or the
  process's own files is reported.
- Deliberately reads the *whole* stack region, not just current call
  frames: a function that already returned doesn't clear the stack bytes
  it used, so this can still find evidence of a hook that ran and returned
  before the scan happened -- the same "recent history, not just this
  instant" property that lets a human watching stacks scroll by over time
  catch things a single live snapshot would miss, approximated here from
  one point-in-time read.
- Unlike the other two signals, this one has NO baseline: a hit is reported
  the first time it's ever seen for a watched process, even on the very
  first check, specifically because the point is finding evidence of
  something that may already have been happening before you started
  watching (the real customer case: exclusions had already been silently
  removed well before anyone looked). The "⚠" stays lit for as long as the
  module remains loaded, same persistence behavior as the new-module
  signal.
- A short, explicitly best-effort, user-extensible list of known AV/EDR
  module-name fragments (McAfee/Trellix, Symantec/Broadcom SEP, CrowdStrike
  Falcon, SentinelOne, VMware Carbon Black, Sophos, Trend Micro, Cylance,
  Palo Alto Cortex XDR) attaches a friendly vendor label when a hit
  matches -- an unmatched hit is still reported as an "unrecognized
  third-party module," not silently dropped, since the list is inherently
  incomplete (vendors rename/version/sometimes randomize module names) and
  an unlabeled hit is still worth investigating.
- This is a pointer-address scan, not true call-stack unwinding (what
  Process Explorer's own "Stack" button does) -- cruder, no call-frame
  structure, but far simpler, and answers the question that actually
  matters: is this vendor's code anywhere near this thread. Framed
  throughout the UI as "worth confirming with Process Explorer/Procmon,"
  not proof on its own.
- THREAD_BASIC_INFORMATION/TEB struct layout verified empirically against
  a real running process before shipping (same standard as the earlier
  SYSTEM_THREAD_INFORMATION work): TebBaseAddress resolved to a real
  address, and its UniqueProcess/UniqueThread fields matched the queried
  PID/TID exactly, confirming the field offsets.
- Windows-only, same constraint as the other two signals.
- Note from real testing: expect some genuinely benign "new module loaded"
  events alongside real findings -- large apps like Chrome delay-load
  Windows OS components on demand well after startup (e.g.
  Windows.Devices.Bluetooth.dll/BthRadioMedia.dll appearing the moment a
  Bluetooth-related API actually gets touched), not just at launch. Normal
  behavior, not a false detection -- the event log's real value is exactly
  this: building a sense of what's ordinary noise for a given process
  versus what's worth a closer look via the stack scan.

✨ NEW Notable column: AV/EDR recognition + process-masquerading heuristic
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Two quick wins pulled off the "Future ideas" backlog. New "Notable"
  column in the main table (and the Parent-processes children window):
  "🛡 <vendor>" when a process name matches the same best-effort
  known-security-vendor list Interference Watch's stack scan already uses
  (now extended with a few more consumer/enterprise names -- Microsoft
  Defender, Malwarebytes, ESET, Kaspersky, Bitdefender), or "🎭 not launched
  by <expected>" for the small set of well-known Windows process names with
  a stable, version-independent expected parent.
- Deliberately conservative on the masquerade side, to avoid the tool
  crying wolf: only svchost.exe/services.exe and lsass.exe/wininit.exe are
  checked. csrss.exe was considered and excluded on purpose -- its real
  parent (smss.exe) exits immediately after creating it, so a naive check
  there would misfire constantly, which would undermine trust in every
  other signal this app reports.
- Both are informational/investigative, not proof: a real security
  product's name match is just a helpful label (a real gap for anyone who
  isn't already familiar with what to look for), and a masquerade mismatch
  is "worth a second look," since an unusual but entirely legitimate launch
  path can also trigger it.
- Blank for the overwhelming majority of rows -- this isn't meant to be a
  busy column.

✨ NEW Memory column (actual RSS bytes, not just Mem%)
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Mem% alone doesn't say how much memory that actually *is* without doing
  the math against total system RAM yourself. New "Memory" column (between
  Mem% and Disk R) shows the real figure -- RSS (Resident Set Size), the
  same value Mem% is already computed from, so this was near-zero-cost to
  add: the raw byte count was already being sampled every tick, just never
  surfaced. Works identically on all three platforms (unlike Private,
  which has real per-OS gaps).
- In the Parent-processes view, Memory shows the same combined
  parent-plus-every-descendant total that Mem%/CPU% already do, not just
  the parent row's own usage -- consistent with how that view's grouped
  totals already work.

✨ NEW Interference Watch: auto-open on watch + at-a-glance severity icons
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Clicking "Watch for Interference" now opens (or brings to focus) the
  Interference Watch window itself, instead of leaving you to separately
  discover the View/Process menu or tray's "Check for Interference." Only
  on *adding* a watch, not removing one.
- Each watched process in the "Watched Targets" list now shows an
  at-a-glance status: "✓" (nothing ever logged against it), "⚠" (everything
  logged so far matched the known-vendor list -- attributable, still worth
  attention but less alarming), or "🛑" (something unrecognized was found --
  the case that most needs a closer look). Individual events in the log get
  the same icon for the same reason, in both the on-screen list and the
  Copy to Clipboard text. A display hint from a best-effort name match, not
  a certified verdict. Directory watches don't get a status icon --
  attributing a directory's *historical* events back to it reliably isn't
  worth the complexity for a glance indicator, so those stay a plain
  listing.

✨ FIXED Children drill-down window stealing focus / reopening itself
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- With "Parent processes only" checked and a parent selected, the children
  window would pop itself back to the front (and try to take focus) on
  every single sample tick -- even seconds after being closed by hand.
  Root cause: the main table re-selects the already-selected row on every
  refresh purely to keep it visually highlighted across the data update,
  but Fyne's Table.Select() unconditionally fires OnSelected even when the
  selection didn't actually change, and the selection handler unconditionally
  reopened/refocused the children window whenever parents-only was on.
  Confirmed via testing to be unrelated to Interference Watch specifically
  (reproduced with no watch active at all) -- purely a "Parent processes
  only" + selection issue. Fixed by only reopening/refocusing on a genuine
  click, not a periodic re-select that's just preserving the highlight.

✨ IMPROVED Thread inspection: column headers, matching the main table
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- "Show Threads" now renders as an aligned table with a header row (TID,
  State, Start Address, Kernel Time, User Time) instead of one free-form
  text line per row -- the field order wasn't obvious to someone who
  hasn't been following this feature's development, and Process
  Explorer's own equivalent view has real columns.

✨ FIXED Silent module-list resolution failures against busy processes
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Every thread's start address showed "N/A" for real, many-threaded
  processes like a live Chrome browser process (48+ threads) -- and
  nothing indicated anything had gone wrong. Root cause: the Toolhelp32
  module snapshot can fail with the well-documented, genuinely transient
  ERROR_PARTIAL_COPY against a busy process, and the previous code only
  checked for the snapshot handle itself failing to open, silently
  swallowing a failed enumeration into an empty-but-successful-looking
  result. Fixed with a bounded retry, and a distinct "couldn't verify this
  time" state (a banner in the Threads window, and a skip rather than a
  false-clean in Interference Watch) instead of treating a failed check as
  a clean one.
- Also added a per-thread NtQueryInformationThread fallback for the rarer
  case where the bulk thread-list API reads a thread's start address as
  exactly 0 (confirmed this happens for a process's own first thread, and
  can happen elsewhere too) -- Process Explorer/Process Hacker's own
  technique for the same gap.

✨ NEW Tooltips throughout the app
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Hover any column header, button, checkbox, or select to see what it does
  -- prompted directly by the new "⚠" Interference Watch column, whose
  purpose isn't obvious from the glyph alone to someone who hasn't been
  following this feature's development.
- Fyne itself has no built-in tooltip support yet (its own changelog says
  so explicitly). Uses github.com/dweymouth/fyne-tooltip, the same
  drop-in package already proven in KrankyBearCommander -- ttwidget.Button/
  Check/Select as drop-in replacements for the equivalent core Fyne
  widgets, plus AddWindowToolTipLayer/DestroyWindowToolTipLayer wired into
  every window (main, children drill-down, Interference Watch, System
  Info, Resource Details).

✨ NEW Grouped CPU%/Mem% totals in the Parent-processes view
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Each parent row now shows the combined CPU%/Mem% across itself and every
  descendant (children, grandchildren, ...) it's standing in for, Task
  Manager-style, instead of just its own usage. The "Top CPU"/"Top Mem"
  threshold filter, when also on, filters against this combined figure so
  it matches what's actually displayed. The children drill-down window
  still shows each child's own individual figures -- the whole point of
  opening it is seeing the breakdown the parent row is summarizing.

✨ NEW Sort/filter/End Process in the children drill-down window
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- The Parent-processes view's children window has graduated from a
  read-only list to the same sortable columns (click a header, click again
  to reverse), a name filter, and End Process (with the same confirmation
  dialog) as the main table -- now that the simpler version has proven the
  overall approach is worth having.
- Switching to a different parent resets the filter and selection (an old
  filter usually doesn't apply to a different parent's children), but
  keeps whatever sort column/direction was already chosen, same as the
  main table keeps its sort across data changes.

✨ NEW Single-instance enforcement
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Launching a second copy now shows a small "already running" window with
  a Quit button instead of opening a second full instance. Deliberately
  simple, per feedback: no cross-process IPC, no bringing the first
  instance's window to the foreground -- just close the new one.
- Detects another instance by comparing executable *path* (not name)
  against every other running process, via gopsutil -- avoids mistaking
  an unrelated process that happens to share a generic name. Excludes this
  process's own parent PID specifically so the Mesa3D Windows fallback's
  self-relaunch (see internal/startup) can't misdetect its own prior,
  mid-exit instance as a second real one.

Version 0.2.0 - August 06, 2026
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ NEW Resource Details window
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Click any of the top-strip mini graphs (CPU/Memory/Disk/Network) to open
  a bigger, clearer view of just that one resource, Task Manager-style: a
  resizable list to switch between the four, one large graph for whichever
  is selected. All four keep recording history in the background even
  while not the one shown, so switching between them never shows a blank
  graph.
- The big graph shows value-axis gridlines (%/KB/s) and a time axis
  (elapsed time back from "now") so peaks and the visible time span are
  actually readable, not just eyeballed.
- Also reachable from the View menu / system tray ("Resource Details"),
  and included in Hide All / Show All like every other secondary window.
- Top consumers list: for CPU, Memory, and Disk, shows the processes
  actually driving that resource (name + its value for just that metric),
  filtered by an adjustable threshold (Off / ≥1% / ≥5% / ≥10% / ≥25% for
  CPU/Mem; Off / ≥10 / ≥100 / ≥500 / ≥1000 KB/s for Disk) rather than a
  fixed "top N", capped at 8 rows regardless of how many clear the
  threshold. Not available for Network -- there's no per-process network
  API to draw from (same gap noted elsewhere in this app).

✨ FIXED Disk/Network graphs could show a garbage auto-scale after launch
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- The very first system sample after launch diffed disk/network byte
  counters against a zero baseline instead of skipping the first tick, so
  it reported the *entire* cumulative bytes read/sent since boot as a
  one-second "rate" -- a huge one-off outlier. Since the graphs auto-scale
  to their own visible max, that single spike dominated the axis (e.g.
  "473199313 KB/s") and squashed all real data into an invisible sliver
  until the outlier aged out of the buffer (2-5 minutes). CPU/Memory were
  never affected (fixed 0-100% scale, immune to this). Now the first
  sample only records a baseline and reports no rate, same as the
  per-process Disk R/W columns already did correctly.

✨ NEW Regex process-name filter
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- "Regex" checkbox next to the name filter switches it from a plain
  substring match to a real regexp match against the process name -- e.g.
  ^(?i)(process|activity).* to compare just this app's own processes
  against Activity Monitor's, with nothing else cluttering the list.
- An invalid or still-being-typed regex fails open (shows every row)
  rather than going blank or erroring.
- Note: this still combines with "Parent processes only" via AND, same as
  the existing Top CPU/Mem filters -- a process with no children of its
  own gets hidden by that view regardless of whether it also matches the
  name filter.
- The filter entry now has its own toolbar row (with Regex, Refresh Now,
  Refresh every), instead of squeezed onto one row with every other
  control -- it was too narrow to type a regex into comfortably.

✨ NEW System Info window
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- A point-in-time summary (View menu / system tray): computer name and
  OS/platform/version/architecture, CPU model/cores/logical processors/
  speed, total memory, mounted disk volumes with size, and network
  adapters with type (wired/wireless), connected status, and WiFi signal
  strength where applicable.
- "Copy to Clipboard" button copies the whole summary as plain text --
  handy for a bug report or comparing against another machine.
- CPU speed shows N/A rather than a misleading "0.00 GHz" when gopsutil
  can't report it. Apple Silicon doesn't return a clean 0 for this --
  empirically it's some other meaningless counter (4 on an M4 Pro), which
  slipped past an initial `<= 0` check; fixed with a "no real CPU has run
  this slow in decades" floor (100 MHz) instead of exactly 0.
- "Copy to Clipboard" now confirms the copy happened -- the button relabels
  to "Copied to Clipboard ✓" and turns green for 2 seconds, since clicking
  it otherwise gave no feedback that anything had happened.
- Network adapter type is a name-pattern heuristic (wlan/eth/en0 etc.),
  not a guaranteed-accurate classification for unusual adapter names.
- WiFi signal strength: a real API on Linux (github.com/mdlayher/wifi,
  pure Go, no shell-out); a standard-tool shell-out on macOS
  (system_profiler) and Windows (netsh) -- neither OS has a comparably
  simple non-shell option (the real alternative is cgo+Objective-C or raw
  WLAN-API syscall struct marshaling, more code and more fragile than a
  one-line shell-out and text parse).
- Disk volumes are mounted filesystems (gopsutil's view), not raw physical
  disks -- there's no simple cross-platform API for the latter.
- The window now opens immediately with a "Collecting system info…" message
  and spinner while gathering runs in the background, instead of the whole
  app appearing to freeze until it's done. On macOS specifically, gathering
  can genuinely take 10+ seconds: system_profiler (used for WiFi signal
  strength) takes ~8s on its own regardless of -detailLevel, and there's no
  fast non-privileged alternative (wdutil info is near-instant but needs
  sudo; the legacy airport binary that used to be fast no longer exists on
  current macOS).
- Fixed: the loading message briefly rendered one character per line. It
  was wrapped in container.NewCenter, which sizes to the child's own
  MinSize -- an unconstrained wrapping Label's MinSize degenerates to
  almost nothing, so it wrapped at the narrowest possible width instead of
  real word boundaries. A plain VBox gives it the container's full width.

✨ NEW Per-process Disk I/O and Private memory columns
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Disk R / Disk W columns show live per-process read/write KB/s (diffed
  between samples, the same way the top-strip Disk graph works). Shows
  N/A for processes owned by another user/system account, same as any
  other permission-restricted field.
- Private column: on Windows this is real Private Bytes (Commit Charge);
  on Linux it's RSS minus shared pages, the standard approximation where
  there's no single "private bytes" figure. Shows N/A on macOS -- the
  closest gopsutil figure there is reserved virtual address space (often
  >1TB for a browser renderer), not actual private memory, so showing it
  would be actively misleading rather than just incomplete.

✨ NEW "Parent processes" view
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- "Parent processes only" checkbox declutters the table down to processes
  worth drilling into: anything with at least one child (e.g. a browser
  with a dozen helper processes), plus anything with no visible parent of
  its own. Everything else (a childless process whose parent IS shown) is
  hidden from this view -- it's still there, just one click away.
- Click a parent row to open (or update) a single read-only "Children"
  window listing that process's direct children with live CPU%/Mem%; only
  one such window is ever open, and clicking a different parent swaps its
  contents into it rather than opening another. No sort/filter/End Process
  in that window yet -- starting simple to prove the approach out first.


Version 0.1.0 - August 5, 2026
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ NEW Cross platform process miner application
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- initial checkin
- Windows, Linux and MacOS process miner application

✨ NEW Resizable, persisted layout
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- draggable divider between the process table and the detail pane, and
  between the detail info and its children list; both positions persist
  across launches alongside the main window size
- long process names are ellipsized to fit the Name column instead of
  overflowing into the next one

✨ NEW Hide All / Show All windows
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Alt+H boss-key hotkey (mirrored in a new Window menu and the system tray)
  hides the main window and any open About/Help/Update windows together;
  Show All brings back exactly that same set

✨ NEW "Top CPU" / "Top Mem" consumer filter
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- Off / >=1% / >=5% / >=10% / >=25%, independently adjustable for CPU and
  Mem, combined with the existing name filter -- narrows a busy process
  list down to whatever's actually using resources
- known gap: no per-process disk or network figures are sampled yet, so
  this covers CPU/Mem only for now

✨ IMPROVED Steady-state CPU usage cut from over 30% to roughly 9%
▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔
- cmdline, start time, and live status are now fetched on demand for only
  the selected process, instead of for every process on every sample tick
- on macOS specifically, this avoids a gopsutil trap: Status() forks a `ps`
  subprocess per call, so sampling it for the whole process list every
  tick meant hundreds of process forks a second
